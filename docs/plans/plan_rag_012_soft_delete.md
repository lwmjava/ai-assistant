# RAG-012：软删除与保留期

> 状态：已拆入 `tasks.yaml`（RAG-012 `backlog`，依赖 RAG-011）
> 分支：`fix/rag-005-006-review`
> 来源：交付排期 A2；评审修复方案阶段 3
> 日期：2026-09-25
> 截止：2026-10-15

在 RAG-011 权限之上，管理员删除改为软删除。保留期 **90 天**（开工前写入 ADR）。成员与对话检索看不到已软删文档；届满后物理清理并再次审计。

## 目标

1. 文档删除标记与删除时间；列表默认隐藏；管理员可查看。
2. 保留期届满的物理清理路径与审计。
3. 前端默认隐藏；管理员可切换查看。

## 非目标

不改 RAG-011 角色矩阵；不做完整状态机（RAG-013）；不立刻物理删除。

## 验收

软删对成员与检索不可见；未满 90 天不物理删；届满清理可追溯；相关 pytest 通过。

## 风险与回滚

L2。回滚删除标记与清理任务。禁止在 RAG-011 未验收前开工。
