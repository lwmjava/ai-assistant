# RAG-013：版本状态机、跨租户二次确认与发布历史版

> 状态：已拆入 `tasks.yaml`（RAG-013 `backlog`，依赖 RAG-012）
> 分支：`fix/rag-005-006-review`
> 来源：交付排期 A3；评审修复方案阶段 4
> 日期：2026-09-25
> 截止：2026-10-23

状态：草稿、待生效、已发布、已替换、已归档。显式发布历史版（同事务内取消旧当前并标目标为当前）。系统管理员跨租户删除/重解析/发布需二次确认（确认记录可查，非完整工单）。前端状态筛选与发布操作。

## 非目标

不做生效日期全量载入性能优化；不做完整工单产品；不改默认向量库为 Milvus。

## 验收

发布后同组仅一个当前版；无二次确认则拒绝跨租户敏感操作；前端可筛选与发布；相关 pytest 通过。

## 风险与回滚

L2。回滚状态字段与发布接口。禁止在 RAG-012 未验收前开工。
