# 架构设计模板

> 技术栈无关。新项目需说明目标架构；存量项目需同时记录现状（As-Is）、目标（To-Be）及迁移路径。所有结论区分“事实/假设/决策”，并附代码、配置、日志、图表或文档证据。

## 1. 文档与范围

- 系统/能力：`<填写>`
- 版本与状态：`<填写>`
- 负责人/评审人：`<填写>`
- 范围：`<填写本设计覆盖和不覆盖的边界>`
- 关联 ADR/PRD/API：`<填写路径或链接>`

## 2. 架构上下文（Context）

- 业务目标：`<填写与产品指标的关系>`
- 系统边界：`<填写系统负责和不负责什么>`
- 外部参与者：`<填写用户、管理员、审核员、第三方系统>`
- 信任边界：`<填写网络、租户、身份、数据和模型边界>`
- 约束：`<填写法规、部署、预算、团队能力、存量技术栈约束>`

建议绘制上下文图，并为每条连线标注协议、认证、数据分类与方向。

## 3. 容器（Container）

| 容器 | 职责 | 技术/运行形态 | 输入/输出 | 数据所有权 | 扩缩容 | 负责人 |
|---|---|---|---|---|---|---|
| `<填写，如 API Gateway>` | `<填写单一职责>` | `<填写或写“待选型，决策见 ADR”>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

Agent Harness 推荐边界：API Gateway → Harness → Agent Runtime/Context/State → Skill Loader → Tool Executor → Service → Repository；Guardrails、Memory、Observability、Evaluation 横切但不得绕过权限和执行边界。

## 4. 组件（Component）

对每个关键容器说明：

### `<填写容器名>`

| 组件 | 职责 | 依赖 | 接口/事件 | 状态 | 失败隔离 |
|---|---|---|---|---|---|
| `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<无状态/状态位置>` | `<填写>` |

必须明确：Agent 只提出动作；Tool Executor 执行；Service 承载业务规则；Repository 独占数据访问；MCP 仅作为受控适配边界。

## 5. 数据流

### 5.1 正常路径

1. `<填写入口、认证、关联 trace_id>`
2. `<填写上下文组装、来源标记、预算控制>`
3. `<填写模型决策、结构化 Action>`
4. `<填写 Guardrail、权限和审批>`
5. `<填写 Tool/Service/Repository 调用>`
6. `<填写状态更新、验证、响应和审计>`

### 5.2 数据清单

| 数据 | 来源 | 分类 | 存储/驻留 | 权限过滤 | 保留期 | 脱敏/加密 |
|---|---|---|---|---|---|---|
| `<填写>` | `<填写>` | `<公开/内部/敏感/受限>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

### 5.3 状态与一致性

`<填写事务边界、幂等键、并发冲突、最终一致性、补偿、重复消息和顺序保证。>`

## 6. 失败路径

| 故障 | 检测证据 | 超时 | 重试策略 | 降级/补偿 | 用户结果 | 告警/审计 |
|---|---|---|---|---|---|---|
| `<模型超时/Tool 超时/依赖不可用/权限拒绝/数据冲突等>` | `<指标/错误码/trace>` | `<填写>` | `<次数、退避、可重试条件>` | `<填写>` | `<稳定错误模型>` | `<填写>` |

必须描述：最大 Agent 步数、不可恢复错误进入 FAILED、人工审核状态、取消语义、断点恢复、重复执行保护和发布回滚。

## 7. 非功能需求（NFR）

| 维度 | SLI | 目标/SLO | 测量方法 | 容量假设 | 失败预算 |
|---|---|---|---|---|---|
| 可用性 | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 时延 | `<P50/P95/P99>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 吞吐 | `<RPS/任务数>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 安全 | `<违规率/漏洞>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 成本 | `<单任务成本>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| 可恢复性 | `<RTO/RPO>` | `<填写>` | `<演练方式>` | `<填写>` | `<填写>` |

另需覆盖可观测性、可维护性、可测试性、兼容性、无障碍、国际化和数据治理（按适用性说明）。

## 8. 存量系统迁移（Brownfield）

- 现状证据：`<填写 Java/Go/Node/PHP/.NET 服务、依赖图、接口、数据库、任务调度和真实流量证据>`
- 兼容契约：`<填写 API、事件、Schema、错误码、认证和行为兼容要求>`
- 接入模式：`<旁路/适配器/绞杀者/模块内演进；填写选择理由>`
- 分阶段迁移：`<填写阶段、开关、双写/影子流量、验证和退出条件>`
- 回滚：`<填写可逆步骤、数据修复和负责人>`

## 9. 安全与治理

- Input/Tool/Permission/Output Guard：`<分别填写策略和执行位置>`
- 风险分级：`<填写 L0-L3 或项目自定义映射>`
- 人工审批：`<填写触发条件、审批身份、超时和审计>`
- Prompt Injection：`<填写外部内容隔离、来源标记和指令优先级>`
- 秘密与隐私：`<填写凭据管理、日志脱敏、长期 Memory 写入限制>`

## 10. 架构决策

| 决策 | 状态 | 候选方案 | 选择与理由 | 代价 | 证据 | ADR |
|---|---|---|---|---|---|---|
| `<填写>` | `<提议/已接受/已废弃>` | `<填写>` | `<填写>` | `<填写>` | `<基准/PoC/事实>` | `<路径>` |

## 11. 验证计划

`<填写架构测试、故障注入、容量测试、安全测试、Agent Evaluation、存量回归、上线观测和验收阈值。>`
